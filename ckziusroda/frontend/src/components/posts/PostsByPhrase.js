import React from 'react'

export const PostsByPhrase = (match) => {
    // const searchPhrase = match.match.params
    console.log(match.match.params)

    return (
        <div>
            PostsByPhrase
        </div>
    )
}

export default PostsByPhrase;